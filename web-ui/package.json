{
  "name": "@ematix/flow-web-ui",
  "version": "0.6.0",
  "private": true,
  "description": "ematix-flow Web UI — run history + restart/rerun/pause actions over the JSON API at /api/*",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.2.0",
    "vite": "^5.0.0"
  }
}
